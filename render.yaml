services:
  - type: web
    name: chhath-puja-wa-server
    env: node
    plan: free
    region: singapore
    buildCommand: npm install --legacy-peer-deps && npx puppeteer browsers install chrome
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20.11.0"